# Feedback record — 2026-09-21

This record keeps the reported outcomes distinct from related merged work. Status changes only after the listed postcondition has been observed on the revision being described. The installed application was verified as `0.2.9-nightly.20260921.35645731983`, matching the nightly tag and manifest for `bf8d24e`; an installed-version mismatch is not the cause of these reports.

| Requested outcome                                                                                       | Code and PR trace                                                                                                                 | Required postcondition                                                                                                                                                                                                                   | Status at `bf8d24e`                    |
| ------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------- |
| Remove irrelevant decorative, status, and template icons inside the marked trigger/row-label rectangle. | #107, `src/renderer/collection/CollectionRail.tsx`, its component tests, and `electron/smoke-workflow.ts`.                        | At the reported narrow collection-picker viewport, the marked label area has no redundant badges or icons. The existing rename and archive/restore action buttons outside that area remain reachable; a capture records both conditions. | Not implemented by this revision.      |
| A failed Paste action appears as an unobstructed bottom-right notification.                             | `src/renderer/App.tsx` currently sets the paste failure; `error-banner` in `src/renderer/styles.css` currently renders the error. | With a clipboard that contains no image, triggering Paste produces a readable bottom-right notification without clipping the workspace.                                                                                                  | Not implemented by this revision.      |
| Local agent access text wraps without clipping.                                                         | #108, `src/renderer/settings/SettingsView.tsx`, `src/renderer/styles.css`, and `electron/ui-feedback-smoke.ts`.                   | At the reported Settings viewport and scale, the complete setup text is visible or reachable by normal scrolling, with no clipped lines.                                                                                                 | Postcondition failed on this revision. |

The next PR that changes a row must replace the status with its tested commit SHA, platform, command or walkthrough, and retained capture location. Passing unrelated CI checks does not change this record.

## Follow-up revision `d2db35c1305d0a132c27aef45ec5ce36dc6efd1b`

The `bf8d24e` diagnoses above remain the record for that revision. On Windows, `corepack pnpm build` and `corepack pnpm smoke` passed 26 assertion groups in 192,840 ms on this exact revision. The retained evidence is `D:\Code\imnota-verification-artifacts-feedback-acceptance7-20260921\verification-report.json` and five feedback captures under that directory. Earlier CI passed the full suite and static checks; PR #111 must still pass its configured CI gates for the final branch revision.

| Requested outcome                                                           | Candidate path and prepared evidence                                                                                                                                                                                                                                                                            | Native postcondition and capture                                                                                                                                 | Status             |
| --------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------ |
| F1 — remove only the redundant label-area icons while retaining row actions | `1027101`, `CollectionRail.tsx`, and `captureCollectionPicker` geometry assertions: one trigger icon; no row-option icons; labels stay within the option; rename/archive actions sit to the right; no horizontal overflow. These geometry assertions are distinct from the 14 approved Windows baseline images. | `1080x800-feedback-normal-collection-picker.png` and `1080x800-feedback-template-collection-picker.png` show normal and template rows with the retained actions. | Passed on Windows. |
| F2 — show the empty-clipboard failure as a bottom-right notification        | `e2d6d8d`, `App.tsx`, and `ui-feedback-smoke.ts` check the alert role, fixed positioning, exact message, no competing success toast, and viewport margins.                                                                                                                                                      | `1080x800-feedback-paste-error.png` shows the fully visible notification after its entrance animation settles.                                                   | Passed on Windows. |
| F3 — keep local-agent setup copy and the `--mcp` token visible              | `769dbe6`, `SettingsView.tsx`, `styles.css`, and `captureAgentAccess` geometry assertions check copy/toggle separation, no overflow, and a single visible token rectangle. These assertions are distinct from the 14 approved Windows baseline images.                                                          | `agent-access-setup-prompt.png` and `900x800-110pct-feedback-agent-access.png` cover the ordinary 1080 × 800 view and the 900 × 800, 110% zoom stress view.      | Passed on Windows. |
