# Release PR review — 2026-09-05

Scope: the eight open Dependabot PRs, plus the pending feedback workflow and manual update controls. The release candidate is 0.2.0. No branch protections are disabled.

| PR                           | Review result                                                                                                                                                                                                                                                  |
| ---------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| #1 upload-artifact 7         | Workflow-only diff; request bot rebase and rerun current CI before approval/merge.                                                                                                                                                                             |
| #2 checkout 7                | Workflow-only diff; request bot rebase and rerun current CI before approval/merge.                                                                                                                                                                             |
| #3 action-gh-release 3       | Reviewed e87bf80. Same configured release inputs, Node 24 runtime supported by hosted runners; quality and all platform packages passed. Approved and squash-merged as 0c3cb195.                                                                               |
| #4 Electron 44               | Do not merge this version-only change: its CI reports `Clipboard.readImage` no longer exists. The current release candidate uses the validated Electron 39 update already in the workspace. Electron 44 needs clipboard API migration and native revalidation. |
| #5 react-konva 19            | Needs renderer regression validation. Its declared peers include React 18.3.1, so React 18 alone is not a reason to reject it. Keep the validated 18 adapter for this release unless the updated PR passes the annotation workflow.                            |
| #6 Zod 4                     | Needs an explicit schema migration: current code uses single-argument `z.record` and Zod 3 schema contracts. Do not merge a version-only change into persistence/IPC validation.                                                                               |
| #7 React Hooks lint plugin 7 | Expands recommended rules to compiler-oriented checks; needs lint validation and source fixes rather than silently disabling new rules.                                                                                                                        |
| #8 Tailwind 4                | The repository uses the Tailwind 3 PostCSS plugin and directives. The PR only changes the package version; it needs a coordinated PostCSS/style migration.                                                                                                     |

Dependency-review jobs currently fail because Dependency Graph is disabled for the repository, not because those action-only PRs introduce an application advisory. Production dependency audit of the release candidate reported no known vulnerabilities. Enable Dependency Graph in repository security settings to restore that independent check; do not hide its failure with `continue-on-error`.

Main requires up-to-date `quality`, one approving review, last-push approval, conversation resolution and linear history; administrators are included. A feature PR authored by the authenticated owner still needs approval from another account. Publishing 0.2.0 is gated on that review and merge, followed by a tag on the exact approved main commit.
